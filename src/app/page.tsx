/**
 * Main Homepage Component
 *
 * The root page component for the CoverMe application. This serves as the entry point
 * for the Next.js application and renders the main Input component where users can
 * upload their resume, provide job links, and generate cover letters.
 *
 * @fileoverview Homepage component that renders the main application interface
 * @version 1.0.0
 * @author CoverMe Team
 */

import Input from "./input"

export default function Home() {
	return <Input />
}
